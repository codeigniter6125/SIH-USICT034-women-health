rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Deny all direct client read and write access.
    // Patient health data must only be accessed through the FastAPI backend
    // using the Firebase Admin SDK.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
